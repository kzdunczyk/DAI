# Colab-specific requirements for DAI lecture notebooks.
#
# Only installs packages that are NOT pre-installed in Google Colab.
# Deliberately excludes numpy, scipy, requests, cryptography, pandas,
# matplotlib, scikit-learn, tensorflow, keras, plotly, seaborn, etc.
# so that Colab's pinned versions are never upgraded.
#
# Upgrading numpy requires a kernel restart and breaks pre-compiled
# binary extensions (scipy, numba, …) via ABI incompatibilities.
# Upgrading requests/cryptography conflicts with google-colab, pyopenssl,
# and pydrive2.
#
# For local reproducible installs use `uv sync` with uv.lock instead.

mglearn>=0.2.0
dowhy
empiricaldist>=0.7.2
stargazer
wrds
newsapi-python
plotnine
tables
